Stryker output is buffered by the PowerShell wrapper, so always wait for the project reports rather than treating silence as completion.

Always let the remaining projects finish before validating totals and classifications.
